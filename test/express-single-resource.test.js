// just getting npm test to pass
assert = require('assert');
describe('nothing to see here yet', () => {
    it('this is going to work', done => {
        assert.equal(1, 1);
        done();
    });
});